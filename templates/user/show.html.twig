{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <h1>Profil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id :</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Prénom :</th>
                <td>{{ user.firstname }}</td>
            </tr>
            <tr>
                <th>Nom :</th>
                <td>{{ user.lastname }}</td>
            </tr>
            <tr>
                <th>Nom du petit canard :</th>
                <td>{{ user.duckname }}</td>
            </tr>
            <tr>
                <th>Email :</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Mot de passe :</th>
                <td>{{ user.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-secondary mb-2">Modifier</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
