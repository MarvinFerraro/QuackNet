{% extends 'base.html.twig' %}

{% block title %}Nouvel utilisateur {% endblock %}

{% block body %}
    <h1>Création de compte</h1>

    <div class="d-flex align-items-center flex-column">
        {{ form_start(form) }}

        <div class="mb-4 p-2 d-flex align-items-center flex-row flex-wrap">
            {{ form_widget(form) }}
        </div>

        <div class="d-flex justify-content-center flex-column">
            <button class="btn btn-secondary mb-2">{{ button_label|default('Sauvegarder') }}</button>
            {{ form_end(form) }}

            <a href="{{ path('quack_index') }}" class="btn btn-outline-secondary">Accueil</a>
        </div>
    </div>

{% endblock %}
